services:
  - type: web
    name: cdb-monitor
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: python app.py
    envVars:
      - key: DATABASE_URL
        sync: false
