services:
  - type: worker
    name: competitor-monitor
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python scraper.py"
    envVars:
      - key: DATABASE_URL
        sync: false
